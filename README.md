# FuzzySearch.py
fuzzy base search algorithm


How to use:

1. import
2. create object of this class (number of mismatches)
3. use search method (1 param: array of all elements, 2 param: seeker element)